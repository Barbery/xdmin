<div class="pull-right">
    <div class="btn-group mr10">
        <a href="{{ route($route_name ?? "{$route}.create") }}" class="btn btn-primary btn-sm">
            <i class="glyphicon glyphicon-plus"></i> {{ $btn_label ?? '新建' }}</a>
    </div>
</div><!-- pull-right -->
